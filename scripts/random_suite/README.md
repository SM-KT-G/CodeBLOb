# Random Suite

This folder collects bite-sized Python scripts that showcase different features
of the `random` module. Every script is self-contained and runnable via
`python script_name.py [options]`.
